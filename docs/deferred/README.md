# 公众号原创声明待接入补丁

wechat-originality.patch.json 完整保存了本轮审查前的未提交改动，不参与构建。补丁默认声明文字原创，与项目要求的“用户本次明确确认”不一致；新增字段也尚未完成真实草稿回读验证。后续接入应将确认值贯穿工作台、冻结包与适配器，并在真实回归通过后启用。当前运行代码沿用不主动声明原创的适配器。

归档 JSON 的 `lines` 字段按换行连接即可还原原始 Git 补丁；采用 JSON 保存是为了保留补丁上下文中的空格，同时通过源码的尾随空格检查。
