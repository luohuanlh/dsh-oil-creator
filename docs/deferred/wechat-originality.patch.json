{
  "format": "git-diff",
  "lines": [
    "diff --git a/docs/implementation.md b/docs/implementation.md",
    "index 45f14bb..18f1f04 100644",
    "--- a/docs/implementation.md",
    "+++ b/docs/implementation.md",
    "@@ -83,7 +83,7 @@",
    " ",
    " ## 微信公众号图文",
    " ",
    "-微信公众号适配器只接受文章 + 封面冻结包。Host 把 Markdown 安全转为基础 HTML，并把所选封面编码为机械执行输入。Ego 在已登录的同源页面中上传封面、调用草稿创建接口，随后打开返回的草稿编辑 URL，核对 `appmsgid` 和标题；只有回读验证成功才写入 `draft`。返回 Host 的 URL 会移除会话 token，避免凭据进入 overlay。2026-08-23 真实回归得到 `appmsgid=503330667`，标题回读一致；脚本中不存在群发或最终发表调用。",
    "+微信公众号适配器只接受文章 + 封面冻结包。Host 把 Markdown 安全转为基础 HTML，并把所选封面编码为机械执行输入。Ego 在已登录的同源页面中上传封面、调用草稿创建接口，随后打开返回的草稿编辑 URL，核对 `appmsgid`、标题和文章设置；只有回读验证成功才写入 `draft`。原创声明固定为文字原创，作者从当前登录公众号的 `nick_name` 读取并要求 1–8 个字，白名单为空且快捷转载关闭；创作来源固定为“个人观点，仅供参考”。返回 Host 的 URL 会移除会话 token，避免凭据进入 overlay。2026-08-23 真实回归得到 `appmsgid=503330667`，标题回读一致；2026-08-26 已用 Ego 对现行编辑器完成只读字段取证，新增声明字段仍需下一次真实草稿回归确认。脚本中不存在群发或最终发表调用。",
    " ",
    " ## 百家号图文",
    " ",
    "diff --git a/docs/verification.md b/docs/verification.md",
    "index ed9e1bd..4d429a0 100644",
    "--- a/docs/verification.md",
    "+++ b/docs/verification.md",
    "@@ -19,6 +19,7 @@",
    " | Harness 主按钮到 UI 回显的完整黄金路径 | 主按钮、AI、冻结、Ego READY、远端保存、overlay 与 UI 回显全链路 | 已证明 |",
    " | 多图文平台资源边界 | 服务级并发固定为 2；三平台契约测试证明第三项等待前项完成；运行器测试覆盖 512 KiB 输出尾部、10 分钟超时终止、远端 task space 独立关闭及本地/登录页面保留 | 已证明 |",
    " | 微信公众号远端草稿与 id 回读 | 真实 `appmsgid=503330667`，重新打开草稿后标题“测试文章 001”一致 | 已证明 |",
    "+| 微信公众号原创与创作来源默认值 | 2026-08-26 Ego 只读取证确认文字原创、作者“短线观市”、空白名单、快捷转载关闭及“个人观点，仅供参考”=`claim_source_type=4`；`articleDraftScript.test.ts` 覆盖提交字段和设置回读失败门禁 | 本地已证明，远端待复测 |",
    " | 百家号远端草稿与 id 回读 | 真实 `article_id=1874282284679284233`，封面上传、编辑 URL 与标题回读一致 | 已证明 |",
    " | 维科网远端草稿与 id 回读 | 真实 `id=2186865`，必填关键字/行业、草稿列表与编辑页标题回读一致 | 已证明 |",
    " | 第二个视频平台远端草稿闭环 | 抖音 `READY` → “暂存离开” → draft 标题与 `video_id` 回读 | 已证明 |",
    "diff --git a/scripts/article/platforms/wechat-mp.mjs b/scripts/article/platforms/wechat-mp.mjs",
    "index d4f40a2..379bb89 100644",
    "--- a/scripts/article/platforms/wechat-mp.mjs",
    "+++ b/scripts/article/platforms/wechat-mp.mjs",
    "@@ -16,6 +16,9 @@ registerArticleAdapter({",
    " ",
    "     const inspection = await js(String.raw`(() => {",
    "       const html = document.documentElement.innerHTML;",
    "+      const wxState = typeof window === 'object' && window !== null",
    "+        ? window.wx || {}",
    "+        : {};",
    "       const token = new URL(location.href).searchParams.get('token')",
    "         || html.match(/data:\\s*\\{[\\s\\S]*?t:\\s*[\"']([^\"']+)[\"']/)?.[1]",
    "         || '';",
    "@@ -23,16 +26,27 @@ registerArticleAdapter({",
    "       const userName = html.match(/user_name:\\s*[\"']([^\"']+)[\"']/)?.[1] || '';",
    "       const svrTime = html.match(/time:\\s*[\"'](\\d+)[\"']/)?.[1]",
    "         || String(Math.floor(Date.now() / 1000));",
    "+      const authorName = [",
    "+        wxState.cgiData?.nick_name,",
    "+        wxState.cgiData?.nickname,",
    "+        wxState.data?.nick_name,",
    "+        document.querySelector?.('.appmsg_account_name')?.textContent,",
    "+        document.querySelector?.('.weui-desktop-account__nickname')?.textContent,",
    "+        html.match(/(?:nick_name|nickname):\\s*[\"']([^\"']+)[\"']/)?.[1],",
    "+      ].map(value => String(value || '').trim()).find(Boolean) || '';",
    "       return {",
    "         ok: Boolean(token && ticket && userName),",
    "         token,",
    "         ticket,",
    "         userName,",
    "         svrTime,",
    "+        authorName,",
    "         evidence: {",
    "           hasToken: Boolean(token),",
    "           hasTicket: Boolean(ticket),",
    "           hasUserName: Boolean(userName),",
    "+          hasAuthorName: Boolean(authorName),",
    "+          authorNameLength: [...authorName].length,",
    "         },",
    "       };",
    "     })()`);",
    "@@ -41,6 +55,13 @@ registerArticleAdapter({",
    "         evidence: inspection?.evidence,",
    "       });",
    "     }",
    "+    if (typeof inspection.authorName !== 'string'",
    "+      || inspection.authorName.trim() === ''",
    "+      || [...inspection.authorName.trim()].length > 8) {",
    "+      throw articleFailure(\"无法读取符合原创声明要求的微信公众号作者名（1-8个字）\", {",
    "+        evidence: inspection?.evidence,",
    "+      });",
    "+    }",
    "     return inspection;",
    "   },",
    " ",
    "@@ -106,7 +127,7 @@ registerArticleAdapter({",
    "         related_video0: '',",
    "         is_video_recommend0: '-1',",
    "         title0: input.title,",
    "-        author0: '',",
    "+        author0: credentials.authorName,",
    "         writerid0: '0',",
    "         fileid0: coverFileId,",
    "         digest0: input.summary,",
    "@@ -131,14 +152,16 @@ registerArticleAdapter({",
    "         vid_type0: '',",
    "         show_cover_pic0: '1',",
    "         shortvideofileid0: '',",
    "-        copyright_type0: '0',",
    "+        copyright_type0: '1',",
    "+        is_cartoon_copyright0: '0',",
    "         releasefirst0: '',",
    "         platform0: '',",
    "         reprint_permit_type0: '',",
    "-        allow_reprint0: '',",
    "-        allow_reprint_modify0: '',",
    "+        allow_fast_reprint0: '0',",
    "+        allow_reprint0: '0',",
    "+        allow_reprint_modify0: '0',",
    "         original_article_type0: '',",
    "-        ori_white_list0: '',",
    "+        ori_white_list0: JSON.stringify({ white_list: [] }),",
    "         free_content0: '',",
    "         fee0: '0',",
    "         ad_id0: '',",
    "@@ -155,6 +178,9 @@ registerArticleAdapter({",
    "         share_voice_id0: '',",
    "         insert_ad_mode0: '',",
    "         categories_list0: '[]',",
    "+        claim_source_type0: '4',",
    "+        is_user_no_claim_source0: '0',",
    "+        claim_source0: JSON.stringify({ is_user_no_claim_source: 0 }),",
    "       });",
    "       const saveUrl = 'https://mp.weixin.qq.com/cgi-bin/operate_appmsg?t=ajax-response&sub=create&type=77'",
    "         + '&token=' + encodeURIComponent(credentials.token) + '&lang=zh_CN';",
    "@@ -183,25 +209,73 @@ registerArticleAdapter({",
    "     return assertSaved(created, \"微信公众号草稿创建失败\");",
    "   },",
    " ",
    "-  async verify({ input: articleInput, saved }) {",
    "+  async verify({ input: articleInput, inspection, saved }) {",
    "     await openOrReuseTab(saved.draftUrl, { wait: true, timeout: 30 });",
    "     await wait(3);",
    "-    const verification = await js(String.raw`((expectedTitle, expectedId) => {",
    "+    const verification = await js(String.raw`((expectedTitle, expectedId, expectedAuthor) => {",
    "       const values = [...document.querySelectorAll('input,textarea')]",
    "         .map(element => String(element.value || '').trim())",
    "         .filter(Boolean);",
    "       const text = String(document.body?.innerText || '');",
    "+      let articleData = {};",
    "+      try {",
    "+        const jquery = typeof window === 'object' ? window.jQuery || window.$ : undefined;",
    "+        const articleItem = typeof document.querySelector === 'function'",
    "+          ? document.querySelector('.js_appmsg_item')",
    "+          : null;",
    "+        articleData = articleItem && jquery",
    "+          ? jquery(articleItem).data('article')?.data?.getData?.() || {}",
    "+          : {};",
    "+      } catch {}",
    "+      const whitelistIsEmpty = value => {",
    "+        if (Array.isArray(value)) return value.length === 0;",
    "+        if (value === undefined || value === null || String(value).trim() === '') return true;",
    "+        try {",
    "+          const parsed = typeof value === 'string' ? JSON.parse(value) : value;",
    "+          return Array.isArray(parsed?.white_list) && parsed.white_list.length === 0;",
    "+        } catch {",
    "+          return false;",
    "+        }",
    "+      };",
    "       const titleMatched = values.includes(expectedTitle) || text.includes(expectedTitle);",
    "       const idMatched = new URL(location.href).searchParams.get('appmsgid') === expectedId;",
    "       const loggedOut = /扫码登录|请使用微信扫描二维码|请先登录/.test(text);",
    "+      const authorMatched = String(articleData.author || '').trim() === expectedAuthor",
    "+        || values.includes(expectedAuthor)",
    "+        || text.includes(expectedAuthor);",
    "+      const originalMatched = (",
    "+        Number(articleData.copyright_type) === 1",
    "+        && Number(articleData.is_cartoon_copyright || 0) === 0",
    "+      ) || /文字原创/.test(text);",
    "+      const whitelistEmpty = whitelistIsEmpty(articleData.ori_white_list)",
    "+        && !/已设置白名单/.test(text);",
    "+      const fastReprintDisabled = Number(articleData.allow_fast_reprint || 0) === 0",
    "+        && Number(articleData.reprint_type || 0) === 0",
    "+        && (/快捷转载\\s*未开启/.test(text)",
    "+          || articleData.allow_fast_reprint !== undefined",
    "+          || articleData.reprint_type !== undefined);",
    "+      const creationSourceMatched = Number(articleData.claim_source_type) === 4",
    "+        || text.includes('个人观点，仅供参考');",
    "       return {",
    "-        verified: titleMatched && idMatched && !loggedOut,",
    "+        verified: titleMatched",
    "+          && idMatched",
    "+          && authorMatched",
    "+          && originalMatched",
    "+          && whitelistEmpty",
    "+          && fastReprintDisabled",
    "+          && creationSourceMatched",
    "+          && !loggedOut,",
    "         titleMatched,",
    "         idMatched,",
    "+        authorMatched,",
    "+        originalMatched,",
    "+        whitelistEmpty,",
    "+        fastReprintDisabled,",
    "+        creationSourceMatched,",
    "         loggedOut,",
    "         url: location.href,",
    "       };",
    "-    })(${JSON.stringify(articleInput.title)}, ${JSON.stringify(saved.remoteId)})`);",
    "+    })(${JSON.stringify(articleInput.title)}, ${JSON.stringify(saved.remoteId)}, ${JSON.stringify(inspection.authorName)})`);",
    "     if (verification?.verified !== true) {",
    "       throw articleFailure(\"微信公众号未通过草稿页面验证\", {",
    "         status: \"REMOTE_UNVERIFIED\",",
    "diff --git a/tests/articleDraftScript.test.ts b/tests/articleDraftScript.test.ts",
    "index 09a8dd7..02aa88d 100644",
    "--- a/tests/articleDraftScript.test.ts",
    "+++ b/tests/articleDraftScript.test.ts",
    "@@ -58,7 +58,9 @@ interface FixtureRun {",
    "   stderr: string;",
    " }",
    " ",
    "-async function runFixture(scenario: \"success\" | \"login\" | \"verification-failure\" | \"open-error\"): Promise<FixtureRun> {",
    "+async function runFixture(",
    "+  scenario: \"success\" | \"login\" | \"verification-failure\" | \"settings-verification-failure\" | \"open-error\",",
    "+): Promise<FixtureRun> {",
    "   try {",
    "     const result = await execFileAsync(process.execPath, [runtimeFixture, articleDraftScript, scenario]);",
    "     return { code: 0, stdout: result.stdout, stderr: result.stderr };",
    "@@ -378,6 +380,15 @@ describe(\"微信公众号 Ego 草稿脚本\", () => {",
    "     expect(source).toContain(\"content0\");",
    "   });",
    " ",
    "+  it(\"默认填写文字原创、当前作者和个人观点来源，不开放转载\", () => {",
    "+    expect(source).toContain(\"copyright_type0: '1'\");",
    "+    expect(source).toContain(\"is_cartoon_copyright0: '0'\");",
    "+    expect(source).toContain(\"author0: credentials.authorName\");",
    "+    expect(source).toContain(\"ori_white_list0: JSON.stringify({ white_list: [] })\");",
    "+    expect(source).toContain(\"allow_fast_reprint0: '0'\");",
    "+    expect(source).toContain(\"claim_source_type0: '4'\");",
    "+  });",
    "+",
    "   it(\"创建后回读标题，只有浏览器本地草稿才把页面交给用户\", () => {",
    "     expect(source).toContain(\"verified\");",
    "     expect(source).toContain(\"expectedTitle\");",
    "@@ -416,9 +427,16 @@ describe(\"微信公众号 Ego 草稿脚本\", () => {",
    "           kind: \"save\",",
    "           method: \"POST\",",
    "           title: \"Harness 生成的公众号标题\",",
    "+          author: \"短线观市\",",
    "           summary: \"Harness 生成的摘要\",",
    "           fileId: \"cover-file-9\",",
    "           showCover: \"1\",",
    "+          copyrightType: \"1\",",
    "+          originalType: \"0\",",
    "+          whitelist: JSON.stringify({ white_list: [] }),",
    "+          fastReprint: \"0\",",
    "+          claimSourceType: \"4\",",
    "+          claimSource: JSON.stringify({ is_user_no_claim_source: 0 }),",
    "         },",
    "       ],",
    "     });",
    "@@ -447,6 +465,23 @@ describe(\"微信公众号 Ego 草稿脚本\", () => {",
    "     expect(JSON.stringify(draft)).not.toContain(\"token=\");",
    "   });",
    " ",
    "+  it(\"标题和草稿 id 正确但原创或创作来源未回读时仍拒绝报告成功\", async () => {",
    "+    const result = await runFixture(\"settings-verification-failure\");",
    "+    const draft = jsonLines(result.stdout).find((line) => line.platform === \"wechat-mp\");",
    "+",
    "+    expect(result.code).toBe(4);",
    "+    expect(draft).toMatchObject({",
    "+      ok: false,",
    "+      error: \"微信公众号未通过草稿页面验证\",",
    "+      evidence: {",
    "+        titleMatched: true,",
    "+        idMatched: true,",
    "+        originalMatched: false,",
    "+        creationSourceMatched: false,",
    "+      },",
    "+    });",
    "+  });",
    "+",
    "   it(\"浏览器异常消息包含编辑 URL 时也会移除会话 token\", async () => {",
    "     const result = await runFixture(\"open-error\");",
    "     const draft = jsonLines(result.stdout).find((line) => line.platform === \"wechat-mp\");",
    "diff --git a/tests/fixtures/article-draft-ego-runtime.mjs b/tests/fixtures/article-draft-ego-runtime.mjs",
    "index a2483df..4088190 100644",
    "--- a/tests/fixtures/article-draft-ego-runtime.mjs",
    "+++ b/tests/fixtures/article-draft-ego-runtime.mjs",
    "@@ -25,14 +25,20 @@ const state = {",
    " const document = {",
    "   documentElement: {",
    "     get innerHTML() {",
    "-      return \"<script>window.wx={data:{t:'token-123'},ticket:'ticket-7',user_name:'user-9',time:'1787250000'}</script>\";",
    "+      return \"<script>window.wx={data:{t:'token-123'},ticket:'ticket-7',user_name:'user-9',nick_name:'短线观市',time:'1787250000'}</script>\";",
    "     },",
    "   },",
    "   body: {",
    "     get innerText() {",
    "       if (scenario === \"login\") return \"请使用微信扫描二维码\";",
    "-      if (scenario === \"verification-failure\" && state.draftOpened) return \"草稿编辑 不匹配的标题\";",
    "-      return state.draftOpened ? `草稿编辑 ${input.title}` : \"微信公众号工作台\";",
    "+      const settings = \"文字原创 作者: 短线观市 快捷转载 未开启 个人观点，仅供参考\";",
    "+      if (scenario === \"settings-verification-failure\" && state.draftOpened) {",
    "+        return `草稿编辑 ${input.title}`;",
    "+      }",
    "+      if (scenario === \"verification-failure\" && state.draftOpened) {",
    "+        return `草稿编辑 不匹配的标题 ${settings}`;",
    "+      }",
    "+      return state.draftOpened ? `草稿编辑 ${input.title} ${settings}` : \"微信公众号工作台\";",
    "     },",
    "   },",
    "   querySelectorAll() {",
    "@@ -82,11 +88,18 @@ async function fetch(url, options = {}) {",
    "       kind: \"save\",",
    "       method: options.method,",
    "       title: form.get(\"title0\"),",
    "+      author: form.get(\"author0\"),",
    "       summary: form.get(\"digest0\"),",
    "       body: form.get(\"content0\"),",
    "       fileId: form.get(\"fileid0\"),",
    "       coverUrl: form.get(\"cdn_url0\"),",
    "       showCover: form.get(\"show_cover_pic0\"),",
    "+      copyrightType: form.get(\"copyright_type0\"),",
    "+      originalType: form.get(\"is_cartoon_copyright0\"),",
    "+      whitelist: form.get(\"ori_white_list0\"),",
    "+      fastReprint: form.get(\"allow_fast_reprint0\"),",
    "+      claimSourceType: form.get(\"claim_source_type0\"),",
    "+      claimSource: form.get(\"claim_source0\"),",
    "       url: requestUrl,",
    "     });",
    "     return response({ appMsgId: \"appmsg-42\", base_resp: { ret: 0, err_msg: \"ok\" } });"
  ]
}
